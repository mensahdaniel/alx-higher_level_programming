#!/usr/bin/python3
""" defines a text file reading options"""


def read_file(filename=""):
    """ opens and prints the content of the file in utf-8 to s.o"""
    with open("filename", encoding=utf-8) as fi:
        print(fi.read(), end="")
